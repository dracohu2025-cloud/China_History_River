# 清除浏览器缓存 - 1949年事件显示

## 🚨 问题

用户反馈：强制刷新页面后仍然能看到1949年事件的旗子图标。

**根本原因**: 浏览器缓存了旧版本的JavaScript文件

## 🛠️ 解决方案

### 方法1: 硬刷新 (推荐)

**步骤**:
1. 访问 https://history.aigc.green/
2. 按以下组合键进行**硬刷新**:

   **Windows/Linux**:
   - `Ctrl + Shift + R` (Chrome/Firefox)
   - `Ctrl + F5` (部分浏览器)

   **Mac**:
   - `Cmd + Shift + R` (Chrome/Safari)
   - `Option + Cmd + E` (Safari清空缓存)

3. 验证: 1949年事件应该显示在屏幕顶部，**没有旗子图标**

### 方法2: 清空浏览器缓存 (完全清除)

**Chrome**:
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮 → 选择 "清空缓存并硬性重新加载"
3. 或者:
   - 设置 → 隐私和安全 → 清除浏览数据
   - 选择 "缓存的图片和文件"
   - 时间范围: "全部时间"
   - 点击 "清除数据"

**Firefox**:
1. 按 `F12` 打开开发者工具
2. 设置 → 隐私与安全 → Cookie和网站数据 → 清除数据
3. 刷新页面

**Safari**:
1. Safari → 偏好设置 → 隐私 → 管理网站数据
2. 搜索 `history.aigc.green`
3. 点击 "移除" 或 "全部移除"
4. 刷新页面

### 方法3: 禁用缓存 (开发者工具)

**步骤**:
1. 按 `F12` 打开开发者工具
2. 切换到 **Network** 标签
3. 勾选 **Disable cache** (禁用缓存)
4. 刷新页面
5. 验证1949年事件显示

### 方法4: 使用无痕模式 (最快)

**步骤**:
1. 打开新的无痕/隐私窗口:
   - Chrome: `Ctrl+Shift+N` (Windows) / `Cmd+Shift+N` (Mac)
   - Firefox: `Ctrl+Shift+P` (Windows) / `Cmd+Shift+P` (Mac)
   - Safari: `Cmd+Shift+N`

2. 访问 https://history.aigc.green/

3. **优点**: 无痕模式不使用缓存，100%加载最新代码

4. 验证1949年事件应该**没有旗子图标**

### 方法5: 修改URL参数 (绕过缓存)

访问以下URL（添加参数绕过缓存）:
```
https://history.aigc.green/?v=2
```

浏览器会认为这是新页面，不使用缓存。

### 方法6: 清除DNS和Service Worker缓存

**Chrome**:
1. 访问 `chrome://settings/clearBrowserData`
2. 选择 "高级"
3. 勾选:
   - 缓存的图片和文件
   - Cookie和其他网站数据
   - 网站设置
4. 时间范围: "全部"
5. 清除数据

**Service Worker**:
1. 打开开发者工具
2. Application 标签
3. Service Workers → 点击 "Unregister all"
4. 刷新页面

## 🔍 验证缓存已清除

清除缓存后，在Console中运行：

```javascript
// 检查加载的JS文件名（应该包含新的hash）
console.log('加载的JS文件:');
document.querySelectorAll('script[src]').forEach(s => {
  if (s.src.includes('main-')) {
    console.log(s.src);
  }
});

// 检查1949年事件的DOM元素
document.querySelectorAll('text').forEach(el => {
  if (el.textContent.includes('1949')) {
    console.log('1949年元素:', el.textContent);
    console.log('父元素:', el.closest('g'));
  }
});
```

**预期结果**:
- 加载的JS文件名应该是 `main-Uud9Jl8d.js` (最新构建)
- 1949年元素应该包含 "1949 新中国成立"
- 不应该有任何 `★` 或五角星或旗子相关的HTML元素

## 📊 服务器端验证

在服务器上执行：

```bash
# 检查最新构建时间
date && ls -lh /home/ubuntu/history_river_2025/history_river_November_2025/history_river/dist/assets/main-*.js

# 检查JS文件内容 (应该没有旗子图标)
cd /home/ubuntu/history_river_2025/history_river_November_2025/history_river
grep -c "★" dist/assets/main-*.js  # 应该返回 0

grep -c "1949年·新中国成立" dist/assets/main-*.js  # 应该大于 0
```

## 🎯 成功标准

清除缓存后，页面上应该显示：
- ✅ 1949年事件可见
- ✅ 显示 "1949 新中国成立"
- ✅ **没有**五角星、旗子或任何其他图标
- ✅ 在屏幕顶部约60px位置
- ✅ Zoom In/Out都能看到

## 🚀 终极解决方案

如果尝试了以上所有方法仍然看到旗子图标，说明代码中仍有残留：

**立即操作**:

```bash
# 在服务器上完全重新构建
cd /home/ubuntu/history_river_2025/history_river_November_2025/history_river

# 1. 清理旧构建
rm -rf dist/

# 2. 重新构建
npm run build

# 3. 验证构建输出
ls -lh dist/assets/main-*.js

# 4. 检查文件内容 (确保没有旗子)
grep -c "★" dist/assets/main-*.js  # 必须返回 0

# 5. 重启nginx
sudo nginx -s reload

# 6. 清除浏览器缓存 (方法1-4)
```

---

**请用户完成以下操作**:

1. ✅ 使用开发者工具 → Network → Disable cache
2. ✅ 强制刷新: Ctrl+Shift+R
3. ✅ 或者在无痕模式下打开
4. ✅ 验证1949年事件是否显示（没有旗子）

**如果仍然看到旗子，请提供**: 
- 浏览器类型和版本
- Console日志
- 屏幕截图
- DNS缓存情况
